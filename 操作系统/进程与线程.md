## 进程的组成
### 进程控制块
进程创建时，操作系统为进程分配一个PCB，该结构常驻内存，可随时被系统访问
#### PCB内容
##### 进程标识信息
##### 进程调度信息
##### 资源和内存信息
##### 处理机状态信息
### 程序段
* 进程中可被CPU执行的代码部分
* 程序段本身是静态的，可被多个进程共享
* 多个用户同时运行同一个程序时，内存中只需要保存一份代码
* 各进程通过各自的PCB指向该代码段，节省内存空间
### 数据段
* 包含进程运行所需的各种数据，包括原始输入和执行过程中产生的中间结果与最终输出
* 进程的数据段是私有的，多个进程运行同一程序，数据段也是彼此隔离的，这点与程序段不同

