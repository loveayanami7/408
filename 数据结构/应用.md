## 并查集的应用
### 压缩路径

## 哈夫曼树/编码
### 哈夫曼树的构造
* 本质上是**WPL带权路径长度最小的二叉树**
* 构造方法：**每次选择权值最小的两个结点合并（包括合并后的结点）**
* 哈夫曼树的叶结点是关键字的权值，根结点是所有关键字的权值之和
### 哈夫曼树的性质
* 只有度为0和2的结点 -> n0 = n2 + 1
* 哈夫曼树是不唯一的，但WPL是唯一的
### 前缀编码与哈夫曼编码
* 前缀编码：任何编码都不会是另一个编码的前缀，不会导致歧义
* 如何生成哈夫曼编码：对一课具有n个叶子的哈夫曼树，对树中所有**左支赋0，右支赋1**
* 核心思想：**让频率高的关键字的编码短（离根近，权值高），频率低的关键字编码长（离根远，权值低）**
## 图的应用
### 最小生成树
#### 最小生成树定义
#### Prim
1. 思想：从一个点出发，不断选择当前已选集合到未选集合之间权值最小的边，把新点加入生成树
2. 实现:
#### Kruskal
1. 思想：把所有边按权值从大到小排序，然后依次尝试加入，只要不会形成环就加入
2. 实现：
### 最短路径
#### Dijkstra：单源最短路径
1. 一个起点s到其他所有点的最短路径
#### Floyd：任意顶点间最短路径
1. 任意顶点间的最短路径（动态规划），不断枚举中间节点
### 拓扑排序
1. 每次循环处理入度为0的节点
2. AOV网：顶点biao s
### 关键路径
1. AOE网：顶点表示事件，弧表示活动，权表示活动持续的时间
## 矩阵压缩
