## 存储转发时延计算
### 传输时延和传播时延
### 电路交换、报文交换和分组交换
### 甘特图
## 计算机网络分层模型
### ISO/OSI和TCP/IP模型
#### 相同点
#### 不同点
1. OSI参考模型定义了三个主要概念：**服务、协议和接口**，TCP/IP模型在这三个概念上没有明确区分
2. OSI为7层，TCP/IP为4层
3. OSI先有模型后有协议规范，通用性好；TCP/IP则相反，不适合任何其他的非TCP/IP网络
4. OSI参考模型在网络层支持**无连接和面向连接的通信**，但在传输层仅有**面向连接的通信**；TCP/IP模型在网际层仅有一种**无连接的通信**，但在传输层支持**无连接和面向连接**两种模式